From: Chris Myers <atacs-bugs@shang.elen.utah.edu>
To: killpack@eng.utah.edu
Subject: Re: filenames && different behavior specifications (VHDL) (PR#444)
Date: Tue Jul 11 13:41:55 2000
CC: async@cs

The following command:

atacs <switches> <file1> <file2> ... <fileN> <component>

will now precompile files file1 through file(n-1).  It then will compile fileN
as
the top level using the component named.

Note that if you include more than 1 file in the list, the component is now
required.
